name: Deploy-flask-app

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print test
        run: echo "test"

  deploy:
    runs-on: ubuntu-latest	
    steps:
     - name: Print deploy
       run: echo "deploy"
  
  
